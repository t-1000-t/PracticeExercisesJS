const users = ['Mango', 'Poly', 'Ajax', 'Lux', 'Jay', 'Kong'];
let value;
for (let i = 0; i < users.length; i += 1) {
  value = users[i];
  console.log(`Номер елемента: ${i + 1}, значение елемента: ${value}`);
}
